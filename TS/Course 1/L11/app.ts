// enum type
// - A group of constant
// - that can assign a number to your string and make an easy comparison
// - enum Role [values]

enum Role {
    ADMIN,MANAGER,READ_ONLY_USER
}

console.log(Role);
